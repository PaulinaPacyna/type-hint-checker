
repos:
  - repo: local
    hooks:
      - id: annotation_checker
        name: annotation_checker
        entry: annotation_checker
        language: python
        files: '.py$'
        args: [ --exclude_files=^(?!.*tests/cases).*$, --exit_zero,]
        verbose: true
